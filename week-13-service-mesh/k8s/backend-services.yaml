apiVersion: v1
kind: Service
metadata:
  name: backend-v1
  namespace: mesh-demo
spec:
  selector:
    app: backend
    version: v1
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: backend-v2
  namespace: mesh-demo
spec:
  selector:
    app: backend
    version: v2
  ports:
  - port: 8080
    targetPort: 8080
